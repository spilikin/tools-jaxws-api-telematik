= jax-ws generator for telematik api

NOTE: Currently only PTV3 is supported

== Usage

----
git submodule init
git submodule update
gradle build
----
 
The generated Files can be found in `build/generated`